# usePreviousValue

Принимает переменную (_возможно, props или state_) и возвращает значение предыдущего рендера.

### 💡 Зачем?
